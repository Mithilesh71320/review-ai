"use client";

import { AlertsView } from "@/features/alerts";

/** Route entry — keep thin. Feature logic lives in `src/features/alerts`. */
export default function AlertsPage() {
  return <AlertsView />;
}
