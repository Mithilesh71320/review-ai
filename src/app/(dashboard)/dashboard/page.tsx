"use client";

import { DashboardView } from "@/features/dashboard";

/** Route entry — keep thin. Feature logic lives in `src/features/dashboard`. */
export default function DashboardPage() {
  return <DashboardView />;
}
