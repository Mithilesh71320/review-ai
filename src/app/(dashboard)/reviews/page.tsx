"use client";

import { ReviewsView } from "@/features/reviews";

/** Route entry — keep thin. Feature logic lives in `src/features/reviews`. */
export default function ReviewsPage() {
  return <ReviewsView />;
}
